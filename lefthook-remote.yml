---
pre-commit:
  commands:
    gitleaks:
      run: timeout ${LEFTHOOK_GITLEAKS_TIMEOUT:-30} lefthook-gitleaks {staged_files}

pre-push:
  commands:
    gitleaks:
      run: timeout ${LEFTHOOK_GITLEAKS_TIMEOUT:-30} lefthook-gitleaks {push_files}
